image:
  repository: ghcr.io/maverickd650/stash-s6
  tag: latest@sha256:6bcc87a774b5c423536633dd5dc767e1c6c142682726d58e445cf919eb973b098
  pullPolicy: IfNotPresent
securityContext:
  container:
    readOnlyRootFilesystem: false
service:
  main:
    ports:
      main:
        port: 9999
workload:
  main:
    podSpec:
      containers:
        main:
          env:
            STASH_PORT: "{{ .Values.service.main.ports.main.port }}"
            STASH_STASH: /data
            STASH_GENERATED: /config/generated
            STASH_METADATA: /config/metadata
            STASH_CACHE: /config/cache
persistence:
  config:
    enabled: true
    mountPath: "/config"
  data:
    enabled: true
    mountPath: "/data"
  pip:
    enabled: true
    mountPath: "/pip-install"
portal:
  open:
    enabled: true
